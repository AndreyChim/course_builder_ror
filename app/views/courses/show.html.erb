<%# <h1><%= @course.title %>

<%# <p><%= @course.volume %>

<br>
<h2>Course <%= @course.id %></h2>
<br>
<%# <% @course.units.each do |unit| %> 
  <p>
    <strong>Title:</strong>
    <%= @course.title %>
  </p>

  <p>
    <strong>Volume:</strong>
    <%= @course.volume %>
  </p>

  <p>
    <strong>Price:</strong>
    <%= @course.price %>
  </p>
<%# <% end %> 

<%# <h2>Add a unit:</h2>
<%= form_with model: [ @course, @course.units.build ] do |form| %>
  <%# <p>
    <%= form.label :title %>
    <%# <%= form.text_field :title %> 
  <%# </p> %>
  <%# <p> %>
    <%# <%= form.label :volume %>
    <%# <%= form.text_field :volume %> 
  <%# </p> %>
  <%# <p> %>
    <%# <%= form.submit %> 
  <%# </p> %>
<%# <% end %> 
<br>
<br>
<ul>
  <li><%= link_to "Edit", edit_course_path(@course) %></li>
  <br>
  <li><%= link_to "Destroy", course_path(@course), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>
 <br>
 <br>
<div class="columns">
  <div class="column">
    <h4 class="subtitle is-4 is-pulled-left">List of units:</h4>
  </div>
  <div class="column is-one-fifth">
    <div>
      <%= button_to '+New unit', new_course_unit_path(@course), method: :get %>
    </div>
  </div>
</div>
<div>
  <% @course.units.each do |unit| %>
    <h4 class="subtitle is-4"><%= link_to unit.title , "/courses/#{@course.id}/units/#{unit.id}" %></h4>
  <% end %>
</div>

<br>
<br>
<br>
<div>
  <%= button_to 'Back', courses_path, method: :get %>
</div>
