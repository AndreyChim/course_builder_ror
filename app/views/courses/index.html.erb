<div class="columns">
  <div class="column is-four-fifth is-flex is-flex-direction-column is-justify-content-center">
    <h1 class="has-text-left mb-0 mt-0 pb-0"> Список курсов </h1>
  </div>
  <div class="column is-one-fifth">
    <%# <form method="get" action="/courses/1"> %>
    <%# <a
            href="/courses/1"
            target="_blank"
            class="btn btn-primary"
          >
          New course %>
    <%# </form> %>

          <a href="<%= courses_path(@course) %>" data-role="button" data-icon="info" data-mini="true">New course</a>
  </div>
  </div>
    <hr class="mt-3">

<div class="courses blocks">

  <% @courses.each do |course| %>
    <div class="block">

      <div class="columns">
        <div class="column is-four-fifths">
          <p class="columns">Объем <%= course.volume %></p>
          <a class="is-size-5 columns" href="#"><%= course.title %></a>
        </div>
        <p class="column has-text-right is-size-5"><b><%= course.price %> $</b></p>
      </div>

    </div>
  <% end %>

</div>
<%# <style>
    .column a {
        text-decoration: none;
    }
</style> %>